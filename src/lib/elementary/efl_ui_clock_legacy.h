/**
 * @brief Adds a new clock Widget
 *
 * The default clock format and corresponding strings are based on current locale.
 *
 * @param parent The parent object
 * @return The new object or NULL if it cannot be created
 *
 * This function inserts a new clock widget on the canvas.
 *
 * @ingroup Elm_clock
 */
EAPI Evas_Object *efl_ui_clock_add(Evas_Object *parent);

#include "efl_ui_clock.eo.legacy.h"
